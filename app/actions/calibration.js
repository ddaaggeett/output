export function click_UL(xPos,yPos,windowHeight,windowWidth) {
    return {
        type: 'CLICK_UL',
        xPos,
        yPos,
        windowHeight,
        windowWidth
    }
}
export function click_UR(xPos,yPos,windowHeight,windowWidth) {
    return {
        type: 'CLICK_UR',
        xPos,
        yPos,
        windowHeight,
        windowWidth
    }
}
export function click_LL(xPos,yPos,windowHeight,windowWidth) {
    return {
        type: 'CLICK_LL',
        xPos,
        yPos,
        windowHeight,
        windowWidth
    }
}
export function click_LR(xPos,yPos,windowHeight,windowWidth) {
    return {
        type: 'CLICK_LR',
        xPos,
        yPos,
        windowHeight,
        windowWidth
    }
}
